# Backpack

**Type:** Solana wallet browser extension / xNFT platform (`window.backpack.solana`, `window.xnft.solana`)
**Relevance to IN$DEX:** Supported wallet adapter in `js/indx-wallet.js` (Session 100) alongside Phantom — auto-detected on connect, same API surface (`connect`, `signMessage`, `signAndSendTransaction`, balance reads).
**Status:** Live integration target — no dedicated UI beyond generic wallet-connect flow; not yet referenced elsewhere in second-brain.
