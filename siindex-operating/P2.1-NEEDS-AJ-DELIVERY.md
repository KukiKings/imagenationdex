# P2.1 — needs-aj delivery (email → SMS)

**Status:** Wired in M2M + addresses locked (AJ 2026-08-18)  
**Law:** Every task asks AJ · no auto-approve  
**Priority:** **Email** (full packet) → **SMS** (short alert)

---

## Addresses (AJ provided)

| Role | Address |
|------|--------|
| **Primary (personal)** | `dadyboy73@gmail.com` |
| **IN$DEX ops** | `imagenationdex@gmail.com` |

- Full needs-aj packet → **personal**  
- Ops trail / CC → **imagenationdex@gmail.com**  

Env: `AJ_NOTIFY_EMAIL` · `AJ_NOTIFY_EMAIL_CC` (see `notify.env.example`).

---

## What was built

| Piece | Path |
|-------|------|
| Notify module | `siindex-m2m/notify.mjs` |
| Outbox (always) | `siindex-m2m/outbox/` |
| Env example | `siindex-m2m/notify.env.example` |
| Runner hook | On `needs-aj`, runner calls `notifyNeedsAj` |

## Flow

```
Agent returns needs_aj
  → job.status = needs-aj
  → notify builds packet
  → outbox JSON (always)
  → email → dadyboy73@gmail.com (CC imagenationdex@gmail.com)
  → SMS when Twilio + AJ_NOTIFY_SMS set
  → AJ: PROCEED | HOLD | REJECT | REVISE
  → node runner.mjs authorize <jobId>
```

## Commands

```bash
cd siindex-m2m
node notify.mjs test
node runner.mjs notify-test
node runner.mjs authorize <id>
```

## Live email

Outbox holds subject/body. Transport: Gmail draft/send or SMTP using addresses above.

## Live SMS

Optional — set phone + Twilio when ready.

*She asks · email first · you approve*
