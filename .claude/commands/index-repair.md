Run the supervised IN$DEX Quality and Recovery workflow for this issue:

`$ARGUMENTS`

Required reading:

1. Protected parent `CLAUDE.md`.
2. Current Master Mega-Prompt referenced there.
3. `CLAUDE_AGENT_PROTOCOL.md`.
4. `QUALITY_RECOVERY_PROTOCOL.md`.
5. `CLAUDE_CURRENT_HANDOFF.md`.

Start in `CHECK_ONLY`. Move to `LOCAL_REPAIR` only when AJ's current request authorises a defined repair or implementation scope. This command alone does not authorise commit, push, merge, deployment, migration, real identity issuance, real-value transactions or protected-file changes.

Follow the one-writer rule. Prove workspace identity. Reproduce the failure. Classify the risk and root cause. Make the smallest complete authorised repair. Run focused and affected cross-cutting checks. Finish with `QUALITY_RECOVERY_REPORT_TEMPLATE.md` and the handoff required by `CLAUDE_AGENT_PROTOCOL.md`.

Stop on missing authority, unexplained working-tree changes, another active writer, protected-file scope, remote drift, conflicts, missing credentials or a broader security, privacy, identity or financial risk.
